import { Headset } from 'lucide-react';
import PlaceholderPage from '../components/ui/PlaceholderPage';

export default function AtendimentoPage() {
  return (
    <PlaceholderPage
      icon={Headset}
      titulo="Visão Geral — Atendimento"
      descricao="Painel com métricas de atendimento, conversas ativas, fila e status dos agentes."
    />
  );
}
